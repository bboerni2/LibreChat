endpoints:
  azureOpenAI:
    titleConvo: true
    titleModel: "gpt-5.1-chat"
    summarize: true
    summaryModel: "gpt-5.1-chat"
    groups:
      - group: "azure-foundry-gpt51"
        apiKey: "${AZURE_FOUNDRY_API_KEY}"
        baseURL: "${AZURE_FOUNDRY_BASEURL}"
        serverless: true
        models:
          gpt-5.1-chat: true
