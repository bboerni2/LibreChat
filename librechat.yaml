endpoints:
  azureOpenAI:
    titleConvo: true
    titleModel: "gpt-5.1"
    summarize: true
    summaryModel: "gpt-5.1"

    groups:
      - group: "azure-gpt51"
        apiKey: "${AZURE_OPENAI_API_KEY}"
        instanceName: "${AZURE_OPENAI_INSTANCE}"
        apiVersion: "${AZURE_OPENAI_API_VERSION}"

        models:
          gpt-5.1:
            deploymentName: "${AZURE_OPENAI_DEPLOYMENT_GPT51}"
            default: true
            label: "GPT-5.1 (Azure)"
